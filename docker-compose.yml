services:
  affetta:
    build: .
    ports:
      - "8787:8787"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./config/profiles:/app/config/profiles:ro
    restart: unless-stopped
